/**
 * 테마 전환 시 사용되는 CSS 클래스명
 * - 테마 변경 시 HTML 요소에 임시로 추가되어 부드러운 전환 효과 제공
 */
export const TRANSITION_CLASS = "theme-transition";

/**
 * 동적으로 생성되는 테마 전환 스타일 태그의 ID
 * - 중복 생성 방지를 위해 사용
 */
export const TRANSITION_STYLE_ID = "theme-transition-style";
